# Synthetic data

Keep small, reproducible fixtures in `sample/`. Generated large datasets belong in `generated/`, which is gitignored. Never place real customer or institution event logs in this directory.
