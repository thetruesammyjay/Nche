# Architecture

Nche sits beside an institution's transaction flow. It receives normalized pseudonymous events, evaluates behaviour/device/sequence/transaction/beneficiary signals, and returns an explainable recommendation. The institution retains final authorization authority.
